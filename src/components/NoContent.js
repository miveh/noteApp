import styled from "styled-components";

const NoContent = styled.p`
  color: #fff;
  font-family: Nunito;
  font-size: 20px;
  font-style: normal;
  font-weight: 300;
  line-height: normal;
`;

export default NoContent;
